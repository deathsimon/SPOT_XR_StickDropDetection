[Settings]
# RTSP server address
; rtsp_url = rtsp://localhost:8554/live
# Camera device index
camera_index = 4
# Pixel threshold for detecting a drop
drop_threshold = 1000
# Print all the information (YOLO inference time, Y of the detected object, etc.)
verbose = True
# Send signal to the server when a drop is detected
; IP = 
; port =